Inventory this chunk as part of a whole-project analysis. Return observations relevant to each packaged
requirement and additional specification issues, with exact references. Include protective mechanisms,
potential violations, active routes, connections to other files and facts needed to judge absence.
These are intermediate facts, not final findings. A chunk alone cannot prove whole-project compliance.
Be concise: aggregate related facts by requirement, normally 8-16 observations, each fact under 1200
characters. Do not copy full requirement texts or enumerate every specification clause individually.
For normative documents summarize relevant controls and cite ranges; do not report the written
requirements as missing implementations. Focus code observations on executable protection mechanisms.
Cover all relevant portions of the chunk. Do not claim a violation simply because another chunk is absent.
For files irrelevant to security, an empty observations list is acceptable. Do not repeat boilerplate.
Every evidence path and range MUST be inside allowed_evidence_ranges. Use the line-number prefix
before ':' in the supplied text, not section numbers or numeric values inside that text. DOCX
references use the supplied paragraph numbers. Never cite unseen files or missing lines.
